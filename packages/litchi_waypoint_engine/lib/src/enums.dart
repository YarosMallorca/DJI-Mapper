enum RotationDirection { clockwise, counterClockwise }

enum GimbalMode { disabled, focusPoi, interpolate }

enum AltitudeMode { agl, msl }

enum ActionType {
  stayFor,
  takePhoto,
  startRecording,
  stopRecording,
  rotateAircraft,
  tiltCamera
}
